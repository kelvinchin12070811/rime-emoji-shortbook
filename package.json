{
    "name": "rime-shorthand-emoji",
    "packageManager": "yarn@3.2.1",
    "type": "module",
    "scripts": {
        "build:compile": "tsc",
        "build:main": "node --experimental-json-modules --es-module-specifier-resolution=node ./build/index.js",
        "build": "yarn build:compile && yarn build:main"
    },
    "devDependencies": {
        "@types/node": "^17.0.41",
        "typescript": "^4.7.3"
    },
    "dependencies": {
        "emojibase": "^6.1.0",
        "emojibase-data": "^7.0.1",
        "yaml": "^2.1.1"
    }
}
